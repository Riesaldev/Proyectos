'use strict';

// Función que retorna un valor entero aleatorio entre 0 y un valor máximo.
const randomValue = (max) => Math.floor(Math.random() * (max + 1));

// Exportamos la función.
export { randomValue };

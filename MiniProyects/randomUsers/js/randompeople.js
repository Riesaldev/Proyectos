/**
 * ###################
 * ## Random people ##
 * ###################
 *
 * Dada un API que retorna usuarios de forma aleatoria:
 *
 *      - https://randomuser.me/api?results=10
 *
 * Crea una función que reciba como argumento un número de usuarios y pinte
 * dicho número de usuarios el el <ul> correspondiente del HTML.
 *
 * Cada <li> debería tener una estructura similar a esta:
 *
 * <article>
 *      <header>
 *          <img src="${imagenUsuario}" alt="${nombreCompleto}">
 *          <h1>${nombreCompleto}</h1>
 *      </header>
 *      <p>${ciudad} (${país}), ${añoNacimiento}</p>
 * </article>
 *
 */

'use strict';
